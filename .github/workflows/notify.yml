name: Notify
on:
  schedule:
    - cron: "*/15 * * * *"   # every 15 minutes
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit /api/notify
        run: |
          curl -fsS "${{ secrets.NOTIFY_URL }}?secret=${{ secrets.CRON_SECRET }}"
